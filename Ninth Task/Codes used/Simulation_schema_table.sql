CREATE TABLE user_products (
  user_id       INTEGER,
  product_id    INTEGER,
  product_name  TEXT,
  brand         TEXT,
  category      TEXT,
  price         NUMERIC,
  rating        REAL,
  color         TEXT,
  size          TEXT
);
